//Given an array of strings strs, group all anagrams together into sublists. You may return the output in any order.
//Algorithm for this solution 
// Cleared from the question we have to return list of strings in output
// Check the length of the string if zero, return a empty list
// If String if not empty then we create an Hashmap With String as key and List as its value
// Then we initialize an integer variable named count 
//
